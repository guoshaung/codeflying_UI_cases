# CF-P0-INTL-PC-043 点击删除按钮可成功删除当前密钥

- 优先级：P0
- 模块：海外主站 / 侧边栏 / 设置和账单 / API服务 / API密钥
- 平台：海外PC
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：`api_key_agent`
- 账号类型：default-playwright-session
- 是否修改数据：yes
- 数据锁：`api_key:{account_id}`
- 清理动作：删除本轮创建的测试密钥；恢复基线状态
- 人工测试原因：无
- XMind 原始标记：`priority-1`

## 测试步骤

1. 创建独立的 海外PC Browser Context，并按前置条件加载或清除登录态。
2. 进入 FM 路径：`海外主站 / 侧边栏 / 设置和账单 / API服务 / API密钥`。
3. 先创建一个本轮专用测试密钥，名称建议包含 `autotest`、日期或 run_id，避免删除用户已有真实密钥。
4. 在密钥列表中定位本轮刚创建的测试密钥，记录删除前列表数量或该密钥名称/尾号。
5. 点击该测试密钥所在行的删除按钮，并在二次确认弹窗中点击“确定/确认”。
6. 等待删除成功 toast、列表刷新、该密钥行消失、接口返回删除成功等任一强证据。
7. 记录页面状态、URL、关键 DOM、接口结果和截图。

## 预期结果

1. 原始测试点描述的目标结果正确发生：点击删除按钮可成功删除当前密钥。
2. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- 必须删除“本轮创建的测试密钥”，不要删除存量真实密钥。
- 出现删除成功 toast、该密钥行消失、列表数量减少、删除接口业务成功中的任一强证据即可 PASS。
- 如果删除动作已成功但页面没有明显 toast，应以列表变化/接口成功作为主证据，不应误判 BLOCKED。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 无法创建本轮测试密钥、删除按钮不可用、二次确认后接口失败、测试密钥仍存在且无成功证据，或页面不可继续操作。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例由 `码上飞冒烟测试用例--海外版.md` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
