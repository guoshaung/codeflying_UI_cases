# CF-P0-H5-007 用户输入手机号点击获取验证码，验证码可发送至用户

> **暂不派发（H5_OTP_DEV_DISABLED）**：dev H5 当前没有手机号验证码 UI，本用例在国内 H5 全量中暂时排除，不创建结果、不计入计划数。恢复测试前，删除本标记，并将前端国内 H5 派发表中的 `007-008` 取消注释后再执行。

- 优先级：P0
- 模块：国内H5 / 登录 / 手机号验证码登录
- 平台：国内H5
- 执行方式：暂停自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：固定免费测试手机号 `17710753306`；全量/冒烟中由 PC-004 通过 task 绑定 launcher 唯一触发一次验证码，本 H5 用例只复用其接口/Redis 证据。
- Agent 分组：auth_access_agent
- 账号类型：generated-test-phone + mock-otp
- 是否修改数据：no
- 数据锁：`account:{phone}`
- 清理动作：清理动态测试账号及登录会话
- 人工测试原因：无
- 暂停标记：`H5_OTP_DEV_DISABLED`；恢复条件为 dev H5 提供手机号验证码入口且同 task/seq 可取得 PC mock OTP 复用证据。

## 测试步骤

1. 全量/冒烟并行执行时，先检查本轮是否已有 PC 端 mock OTP 登录证据。
2. 若本轮 PC 端已完成验证码发送与登录验证，通过 task 绑定 launcher 派发 `CF-P0-H5-007` 以复用其证据：

   ```bash
   python3 "<task.execution_contract.launcher.script 绝对路径>" \
     --task-bundle "<task-bundle绝对路径>" \
     --case-ids "CF-P0-H5-007"
   ```

   launcher 只会按 task 合同读取 PC-004 已产生的验证码证据，不会再次发码。记录复用证据并标记为 `COVERED_BY_SINGLE_MOCK_OTP`；不得再次点击“获取验证码”、执行滑块或直接运行 `get_redis_code.py`。
3. 单独专项执行本 H5 用例时，总控也必须先在同一 task/seq 中派发 PC-004；本 H5 case 不得成为第二个发码入口。
4. 记录 PC-004 验证码发送接口业务响应、Redis/mock key、TTL、本 H5 页面状态、URL、关键 DOM 和截图；报告中不得暴露完整验证码。

## 预期结果

1. 全量/冒烟并行执行时，本用例能复用 PC 端 mock OTP 证据并标记为 `COVERED_BY_SINGLE_MOCK_OTP`。
2. 复用的 PC-004 发送接口、Redis 验证码与 TTL 证据有效，且本 H5 case 没有触发第二次发码。
3. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- 目标页面、弹窗、状态或数据变化与测试点描述一致。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 目标结果未出现、出现错误状态、数据不一致或页面不可继续操作。
- 验证码发送接口不可连接，或 Redis 在等待窗口内没有产生该手机号验证码。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。
- 仅当接口或 Redis 确实不可连接时标记 `BLOCKED_MOCK_OTP_UNAVAILABLE`；全量模式已有 PC-004 证据时应记 `COVERED_BY_SINGLE_MOCK_OTP`。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
