# CF-P0-H5-013 做同款

- 优先级：P0
- 模块：国内H5 / 首页应用广场 / 应用卡片三点菜单
- 平台：国内H5
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：remix_agent
- 账号类型：default-playwright-session
- 是否修改数据：yes
- 数据锁：`account:{phone}`
- 清理动作：全量 H5 时将本轮创建副本的 app_id、名称写入 `shared/remix_fixture.json`，由 CF-P0-H5-033 精确删除；单独执行时必须记录清理责任，禁止遗留未登记副本
- 人工测试原因：无

## 测试步骤

1. 创建独立的 国内H5 Browser Context，使用移动端 viewport，并加载 `default-playwright-session`。
2. 打开真实 H5 入口：`https://www.codeflying.net/codeflying_h5/`，进入首页应用广场/公开应用列表区域。remix 不涉及短信验证码，不得进入 dev 环境。
3. 首先切换至“义乌专区”，在“精选义乌商家店铺”区域选择稳定、非转圈、非生成中的应用卡片。优先使用标题为“Ｍolly的饰品店”的可见卡片；若该卡片不存在，再选该区域第一张稳定卡片。不要进入应用详情或预览页。
4. 以卡片标题为锚点，在**标题右侧同一行**点击圆形三点/更多图标；该图标可能是可点击 `svg`、`i` 或普通容器，而非 `button`，禁止只搜索 button 后就判定入口不存在。三点打开的底部半屏菜单应同时展示“分享给朋友”和“做同款”。
5. 底部半屏菜单应出现“分享给朋友 / 做同款”等入口，点击菜单中的“做同款”。
   - 全量 H5 中必须触发创建当前账号拥有的副本，并记录新副本 `app_id` 和名称至 `test/<TEST_SEQ>/domestic/h5/shared/remix_fixture.json`，字段为 `source_case`、`run_id`、`app_id`、`app_name`、`created_at`；该 fixture 仅供 CF-P0-H5-033 删除本轮副本。
   - 若当前账号额度不足，出现明确付费/额度弹窗可按用例前置状态记录为 `BLOCKED_BY_QUOTA`，不得误判为入口失败。
6. 记录页面状态、URL、关键 DOM、接口结果和截图。

## 预期结果

1. 原始测试点描述的目标结果正确发生：做同款入口可用，并进入复制/创建/额度提示的合理流程。
2. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- 目标页面、弹窗、状态或数据变化与测试点描述一致。
- 全量 H5 必须创建副本并写入有效 fixture；未能取得新副本的 `app_id` 和名称不得把 013 写为 PASS。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 做同款入口不存在、点击无响应、没有任何创建/确认/额度提示流程、接口明确失败或页面不可继续操作。
- 未先在“义乌专区”的卡片标题右侧尝试三点入口，或仅因 DOM 未命中 `button` 就判定入口不存在，均视为定位不足，不得写 FAIL。
- 若截图显示测试停留在“工作台/我的应用”的已发布/未发布列表，说明走错入口，应先回到 H5 首页公开应用列表重试，不能据此判定本用例失败。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
