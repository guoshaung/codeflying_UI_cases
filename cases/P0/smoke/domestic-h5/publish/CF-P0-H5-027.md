# CF-P0-H5-027 点击查看应用进入应用预览页

- 优先级：P0
- 模块：国内H5 / 工作台 / 已发布 / 应用操作 / 查看对话过程 / 卡片信息
- 平台：国内H5
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：publish_agent
- 账号类型：default-playwright-session
- 是否修改数据：no
- 数据锁：`app:{app_id}`
- 清理动作：无；只读验证，不点击发布、改名或删除
- 人工测试原因：无

## 测试步骤

1. 复用当前阶段已加载的 H5 page 和已发布稳定应用列表。
2. 先从已发布列表选择有非空会话历史的应用：进入对话过程页后检查历史消息/会话列表和应用卡片 DOM；若当前应用无历史，返回列表换下一个稳定卡片，最多尝试 3 个。不得把空历史卡片直接判失败，也不得为了造数据新建应用。
3. 在有非空会话历史的对话过程页找到应用卡片信息中的「查看应用」入口并点击。
4. 确认跳转到应用预览页，截图并写 result；若所有稳定卡片均无会话历史，写 `BLOCKED_TEST_DATA` 并记录检查过的应用名称。

## 定位约束

- 「查看应用」必须在对话过程页应用卡片容器内定位，不能用首页或全页同名文本误点。
- 每次换卡片只允许一次返回和一次定位；达到 3 个候选仍无非空历史立即收口。

## 执行校验标记（必答）

- 本用例的校验标记：`SNT-76106715`
- 写 result JSON 时必须新增字段 `"sentinel": "SNT-76106715"`，原样照抄，不得改写、截断或凭 case ID 推测。
- 该标记为随机生成，只能从本文件读到。缺失或不符即视为未真正读取用例。

## 预期结果

1. 点击「查看应用」后进入应用预览页。
2. 页面无白屏。

## 通过标准

- 目标页面、弹窗、状态或数据变化与测试点描述一致。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 目标结果未出现、出现错误状态、数据不一致或页面不可继续操作。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
