# CF-P0-H5-028 对话过程页点击发布应用提示去 PC 端发布

- 优先级：P0
- 模块：国内H5 / 工作台 / 已发布 / 应用操作 / 查看对话过程 / 卡片信息
- 平台：国内H5
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：publish_agent
- 账号类型：default-playwright-session
- 是否修改数据：no
- 数据锁：`app:{app_id}`
- 清理动作：无；不得触发实际发布或改变应用状态
- 人工测试原因：无

## 测试步骤

1. 复用当前阶段已加载的 H5 page（已在对话过程页或先通过三点菜单进入）。
2. 在对话过程页点击「发布应用」。
3. 确认出现“请去 PC 端发布吧～”或语义等价的 H5 发布引导 toast，截图并写 result。不得点击具体发布类型或改变应用状态。

## 执行校验标记（必答）

- 本用例的校验标记：`SNT-534234F8`
- 写 result JSON 时必须新增字段 `"sentinel": "SNT-534234F8"`，原样照抄，不得改写、截断或凭 case ID 推测。
- 该标记为随机生成，只能从本文件读到。缺失或不符即视为未真正读取用例。

## 预期结果

1. 出现引导用户前往 PC 端发布的 toast 提示。
2. 页面无白屏。

## 通过标准

- 点击后出现“请去 PC 端发布吧～”或语义等价提示，且应用仍保持原状态即 PASS。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 未出现发布引导提示、出现未处理错误、白屏，或点击后意外改变应用发布状态。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。

## 备注

- H5 发布入口当前设计为引导至 PC；本用例只验证该引导提示，不执行真实发布。
