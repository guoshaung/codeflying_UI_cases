# CF-P0-H5-033 点击弹出二次确认弹窗

- 优先级：P0
- 模块：国内H5 / 工作台 / 已发布 / 应用操作 / 删除应用
- 平台：国内H5
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载会员 Playwright session；读取同一 TEST_SEQ 中 CF-P0-H5-013 写入的 `shared/remix_fixture.json`。fixture 必须含当前 run 的 `source_case=CF-P0-H5-013`、`run_id`、`app_id`、`app_name`；禁止复用或删除已有稳定应用。
- Agent 分组：publish_agent
- 账号类型：default-playwright-session
- 是否修改数据：yes
- 数据锁：`app:{app_id}`
- 清理动作：本用例真实删除 CF-P0-H5-013 fixture 中记录的一次性副本，并确认不存在残留
- 人工测试原因：无

## 测试步骤

1. 只读取一次本轮 `shared/remix_fixture.json`，校验 `source_case`、`run_id`、`app_id`、`app_name`。fixture 缺失、不完整或副本未处于可删除状态时，2 分钟内写 `BLOCKED_TEST_DATA`；不得新建 AI 应用、点击“立即开发”、等待生成、扫描 bundle/API、寻找历史应用或请求重派。
2. 创建独立的国内 H5 Browser Context，加载同一会员账号登录态；打开 `https://www.codeflying.net/codeflying_h5/` 并进入“工作台/我的应用”。
3. 停留在应用列表外层，切换到“已发布” tab，按 fixture 的名称和 app_id 双重精确找到卡片。
4. 点击该卡片应用名称旁的三点/更多操作按钮，打开底部半屏菜单；不要点击卡片主体。
5. 点击“删除应用”，确认二次确认弹窗出现；再次核对弹窗中的应用名称/app_id 后，点击确认删除。
6. 等待删除接口业务成功并刷新列表，验证该 fixture 应用不再出现在“已发布”和“未发布”列表，且应用详情不可继续访问。
7. 保存删除前、确认弹窗、删除后列表截图以及接口业务响应。

## 执行校验标记（必答）

- 本用例的校验标记：`SNT-263FE1D3`
- 写 result JSON 时必须新增字段 `"sentinel": "SNT-263FE1D3"`，原样照抄，不得改写、截断或凭 case ID 推测。
- 该标记为随机生成，只能从本文件读到。缺失或不符即视为未真正读取用例。

## 预期结果

1. CF-P0-H5-013 写出的本轮副本可在已发布列表中按 app_id 和名称精确找到。
2. 删除二次确认弹窗正常出现。
3. 确认删除后接口业务成功，该应用从列表和详情中消失。

## 通过标准

- 必须同时具备删除确认弹窗、删除接口业务成功、删除后列表/详情不存在三类证据才可 PASS；只看到弹窗不足以通过。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- fixture 有效但已发布列表找不到该应用、删除确认弹窗未出现、删除接口失败，或确认删除后应用仍存在。

## 阻塞条件

- fixture 缺失、无效、副本不可删除，或账号已达到在线应用额度且无法准备安全删除的数据。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例允许真实删除，但只能删除 CF-P0-H5-013 fixture 中名称和 app_id 均与本轮记录一致的副本。任何不匹配都必须停止，不得误删。
