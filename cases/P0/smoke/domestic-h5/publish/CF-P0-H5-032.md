# CF-P0-H5-032 点击结束应用发布状态

- 优先级：P0
- 模块：国内H5 / 工作台 / 已发布 / 应用操作 / 取消发布
- 平台：国内H5
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：publish_agent
- 账号类型：default-playwright-session
- 是否修改数据：no（必须拦截终止请求，禁止破坏稳定应用）
- 数据锁：`app:{app_id}`
- 清理动作：无；请求拦截后只验证确认态，不确认取消发布
- 人工测试原因：无

## 测试步骤

1. 复用当前阶段已加载的 H5 page 和已发布稳定应用。
2. 在第一次点击任何菜单项之前，安装精确的终止请求拦截：`/\/(?:api)?app\/termination_app(?:\?|$)/`；若拦截无法安装，立即写 `BLOCKED_SAFETY`，不得继续点击。
3. 点击应用卡片名称行内的三点操作图标，打开半屏菜单；入口必须是目标卡片 `uni-view.teamAppList > uni-view` 内 `uni-view.appBodyText`（兼容 `uni-view.appBody`）的 `uni-image`，禁止全页模糊定位。
4. 点击「取消发布」，检查确认弹窗或语义等价 toast。若请求已被拦截且没有任何确认态，判 `FAIL_NO_CONFIRM`，不要重试、不要放行请求、不要确认破坏性操作。
5. 截图并写 result，最后移除拦截。

## 执行校验标记（必答）

- 本用例的校验标记：`SNT-C9937C00`
- 写 result JSON 时必须新增字段 `"sentinel": "SNT-C9937C00"`，原样照抄，不得改写、截断或凭 case ID 推测。
- 该标记为随机生成，只能从本文件读到。缺失或不符即视为未真正读取用例。

## 预期结果

1. 出现取消发布确认弹窗或 toast。
2. 页面无白屏。

## 通过标准

- 目标页面、弹窗、状态或数据变化与测试点描述一致。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 目标结果未出现、出现错误状态、数据不一致或页面不可继续操作。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。

## 备注

- 本用例必须在 H5 工作台外层卡片列表测试；只验证取消发布入口/确认态，不破坏稳定应用状态。
