# CF-P0-H5-015 点击应用名称后操作按钮展示操作功能半屏弹窗

- 优先级：P0
- 模块：国内H5 / 工作台 / 未发布 / 应用操作
- 平台：国内H5
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：app_lifecycle_agent
- 账号类型：default-playwright-session
- 是否修改数据：yes
- 数据锁：`app:{app_id}`
- 清理动作：清理本轮创建的应用或恢复名称、发布状态
- 人工测试原因：无

## 测试步骤

1. 复用当前阶段已加载的 H5 page。
2. 确认在「未发布」tab，应用列表可见。
3. 点击应用卡片名称旁的三点操作图标。
4. 确认底部半屏菜单弹出，截图并写 result。

## 执行校验标记（必答）

- 本用例的校验标记：`SNT-A5308AF0`
- 写 result JSON 时必须新增字段 `"sentinel": "SNT-A5308AF0"`，原样照抄，不得改写、截断或凭 case ID 推测。
- 该标记为随机生成，只能从本文件读到。缺失或不符即视为未真正读取用例。

## 预期结果

1. 底部半屏菜单弹出，展示分享/查看对话过程/修改名称/发布/删除等入口。
2. 页面无白屏。

## 通过标准

- 目标页面、弹窗、状态或数据变化与测试点描述一致。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 目标结果未出现、出现错误状态、数据不一致或页面不可继续操作。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
