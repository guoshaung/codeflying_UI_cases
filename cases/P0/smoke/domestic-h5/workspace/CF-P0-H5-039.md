# CF-P0-H5-039 点击立即开发按钮进入需求澄清页面

- 优先级：P0
- 模块：国内H5 / 应用创建 / 文本框内输入需求文案
- 平台：国内H5
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；默认使用 `default-playwright-session`。
- Agent 分组：app_lifecycle_agent
- 账号类型：default-playwright-session
- 是否修改数据：yes
- 数据锁：`app:{app_id}`
- 清理动作：本用例只验证进入需求澄清页，不提交生成；通常无新应用需要清理。
- 人工测试原因：无

## 测试步骤

1. 复用当前阶段已加载的 H5 page。
2. 在 H5 首页寻找「立即开发 / 创建新应用」等入口并点击。
3. 如出现选择生成类型卡片，选默认项并确认。
4. 确认进入需求澄清/创建页面，有需求输入框，截图并写 result。

## 执行校验标记（必答）

- 本用例的校验标记：`SNT-0412F31A`
- 写 result JSON 时必须新增字段 `"sentinel": "SNT-0412F31A"`，原样照抄，不得改写、截断或凭 case ID 推测。
- 该标记为随机生成，只能从本文件读到。缺失或不符即视为未真正读取用例。

## 预期结果

1. 进入需求澄清或创建应用页面。
2. 页面存在需求输入框。
3. 页面无白屏。

## 通过标准

- URL 进入创建/需求澄清相关路由，或页面出现需求输入框、提示词气泡、创建应用按钮、需求澄清相关文案之一即可 PASS。
- 如果普通点击失败，但 fallback 通过 JS 事件触发或等价路由进入页面，应标记 `RETRY_PASS`，不得判 FAIL。
- 如果请求确认卡片出现并成功确认，确认后页面可继续输入/生成或出现首条有效反馈，也应判 PASS 或 `RETRY_PASS`。
- 本用例只验证“进入需求澄清页面”，不要求提交需求或等待应用生成完成。

## 失败条件

- 创建入口存在，但所有触发策略均无效，且无法进入任何需求澄清/创建应用页面。
- 进入后页面白屏、报错或无可输入需求的 UI。

## 阻塞条件

- 登录态失效、真实 H5 入口不可用、H5 首页无法渲染、测试环境或必要数据不可用。

## 证据要求

- 操作前后截图。
- 页面 URL。

## 备注

- 该用例应与 CF-P0-H5-040 连续执行：041 进入需求澄清页后，042 直接复用当前页面输入提示词。
