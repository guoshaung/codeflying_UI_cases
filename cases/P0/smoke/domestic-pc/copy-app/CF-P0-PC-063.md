# CF-P0-PC-063 当前可创建应用数量大于等于1时，可将应用复制在我的应用列表中

- 优先级：P0
- 模块：国内主站 / 复制应用 / 做同款
- 平台：国内PC
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：remix_agent
- 账号类型：default-playwright-session
- 是否修改数据：yes
- 数据锁：`app:{app_id}`
- 清理动作：清理本轮创建的应用或恢复名称、发布状态
- 人工测试原因：无

## 测试步骤

1. 创建独立的 国内PC Browser Context，并按前置条件加载或清除登录态。
2. 进入 `/my-home`，记录点击前“我的应用”的 app id 集合，然后打开一条稳定的公开应用详情弹窗。
3. 在可见详情弹窗中定位按钮角色，接受中文 `做同款` 以及英文 `Remix`、`Duplicate`、`Copy`、`Clone`；当前国内免费账户的页面可能显示 `Remix`。
4. 点击该按钮并等待复制接口业务成功，或等待“我的应用”出现点击前不存在的新 app id。
5. 验证新增应用确实出现在“我的应用”；记录页面、接口结果和前后截图。
6. 仅删除本轮新增的 app id，保留点击前已存在的全部应用，并记录清理结果。

## 预期结果

1. 原始测试点描述的目标结果正确发生：当前可创建应用数量大于等于1时，可将应用复制在我的应用列表中。
2. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- 做同款等价按钮可见且可点击，并且复制接口成功或“我的应用”出现新的 app id。
- 清理时仅删除本轮新增应用；已存在应用集合不变。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 截图或 DOM 已显示 `Remix`/做同款等价按钮，但选择器没有命中，属于 runner 选择器失败，不得写 BLOCKED。
- 按钮已点击但复制接口失败，或“我的应用”没有出现新 app id。

## 阻塞条件

- 点击等价按钮后明确出现额度不足/付费弹窗，说明“可创建应用数量大于等于 1”的测试数据前置条件不成立，写 `BLOCKED_TEST_DATA`。
- 测试环境、账号或第三方服务不可用。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- `Remix` 是当前英文界面对“做同款”的实际文案，不属于国外版专用入口。
