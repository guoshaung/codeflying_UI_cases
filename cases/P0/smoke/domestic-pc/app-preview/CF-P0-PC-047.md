# CF-P0-PC-047 应用完成后自动跳转至应用预览页默认展示应用页面

- 优先级：P0
- 模块：国内主站 / 应用预览页 / 应用页面展示
- 平台：国内PC
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：加载对应端的 Playwright session；需要应用时使用稳定、非转圈的测试应用。
- Agent 分组：app_publish_test_agent
- 账号类型：default-playwright-session
- 是否修改数据：no
- 数据锁：`none`
- 清理动作：无；若产生临时数据则按 test_run_id 清理
- 人工测试原因：无

## 测试步骤

1. 创建独立的 国内PC Browser Context，并按前置条件加载或清除登录态。
2. 进入 FM 路径：`国内主站 / 应用预览页 / 应用页面展示`。
3. 在 `/my-apps` 选择稳定、已完成且非 `Developing...` 的应用，点击卡片 `.visuel`，处理同页跳转或 popup。
4. 确认 URL 已进入 `/to-app?app_id=...`，等待默认应用页面内容渲染且加载遮罩消失；记录 app_id 给 PC-048～051 复用。
4. 记录页面状态、URL、关键 DOM、接口结果和截图。

## 预期结果

1. 原始测试点描述的目标结果正确发生：应用完成后自动跳转至应用预览页默认展示应用页面。
2. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- URL 为 `/to-app?app_id=...`，默认应用页面内容可见且没有持续加载遮罩。
- 停留在 `/my-apps` 时不得继续断言，也不得直接写 FAIL；应换一个已完成卡片重试一次。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 目标结果未出现、出现错误状态、数据不一致或页面不可继续操作。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
