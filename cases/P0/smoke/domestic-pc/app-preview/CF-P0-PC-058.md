# CF-P0-PC-058 非会员用户点击永久在线弹出付费弹窗

- 优先级：P0
- 模块：国内主站 / 应用预览页 / 发布 / 网页应用
- 平台：国内PC
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户
- 前置条件：加载固定非会员测试账号 Playwright session；该账号应保持免费/无全天候预览权益状态。
- Agent 分组：app_publish_test_agent
- 账号类型：free_account
- 是否修改数据：no
- 数据锁：`none`
- 清理动作：无；若产生临时数据则按 test_run_id 清理
- 人工测试原因：无

## 测试步骤

1. 创建独立的 国内PC Browser Context，并按前置条件加载或清除登录态。
2. 使用 free_account 进入免费账号自己的稳定应用预览页。
3. 在页面顶部直接断言可见“剩余 X 天”组件；禁止通过 hover、鼠标靠近或其他悬停操作触发显示。
4. 点击“剩余 X 天”，在说明浮层中点击“永久在线”。
5. 验证出现付费/升级/购买弹窗，且应用状态未被置为永久在线。
   - 使用 `free_account`，不要临时修改数据库会员状态。
   - 若当前缺少固定非会员 session，本用例标记 `BLOCKED_ACCOUNT_REQUIRED`，不得用会员账号硬跑。
4. 记录页面状态、URL、关键 DOM、接口结果和截图。

## 预期结果

1. 原始测试点描述的目标结果正确发生：非会员用户点击“永久在线”后弹出付费提示。
2. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- 点击前无需 hover 即可看到“剩余 X 天”组件；点击“永久在线”后出现付费/升级/购买弹窗，或业务接口明确返回需购买/无权益。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 未出现“剩余 X 天”组件或“永久在线”入口；点击后直接进入永久在线状态、未出现付费提示，或页面不可继续操作。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
