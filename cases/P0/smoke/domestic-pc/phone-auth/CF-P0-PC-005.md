# CF-P0-PC-005 输入正确验证码，登录成功

- 优先级：P0
- 模块：国内主站 / 登录 / 手机号验证码登录
- 平台：国内PC
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：固定测试手机号 `19113720926` 已获授权用于验证码登录；与 PC-004 共用同一次脚本取码结果。
- Agent 分组：auth_access_agent
- 账号类型：fixed-test-phone (`19113720926`) + mock-otp
- 是否修改数据：no
- 数据锁：`account:{phone}`
- 清理动作：清理动态测试账号及登录会话
- 人工测试原因：无

## 测试步骤

1. 创建独立的 国内PC Browser Context，清除登录态，打开 `https://dev.codeflying.net`。
2. 进入手机号验证码登录页，填写固定测试手机号 `19113720926`。
3. 通过本轮 task 绑定 launcher 派发 `CF-P0-PC-005`：

   ```bash
   python3 "<task.execution_contract.launcher.script 绝对路径>" \
     --task-bundle "<task-bundle绝对路径>" \
     --case-ids "CF-P0-PC-005"
   ```

   launcher 只会按 task 合同以 code-only 方式复用 PC-004 本轮已产生的验证码和 execution receipt；不得再次发码，也不得直接运行 `get_redis_code.py`。
4. 将读取到的验证码填入验证码输入框，勾选用户协议，点击登录；如出现隐私协议弹窗，点击“同意并继续”后只提交一次。
6. 验证登录后当前用户状态、受保护页面访问能力和登录态落盘结果。
7. 记录登录接口业务响应、页面状态、URL、关键 DOM 和截图；报告中不得暴露完整验证码。

## 预期结果

1. 使用 mock/Redis 读取到的正确验证码后登录成功。
2. 页面跳转到登录后页面，当前用户接口或受保护页面验证通过。
3. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- 目标页面、弹窗、状态或数据变化与测试点描述一致。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 目标结果未出现、出现错误状态、数据不一致或页面不可继续操作。
- mock/Redis 取码成功但登录接口业务失败，或登录后无法访问受保护页面。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。
- 仅当 PC-004 的接口/Redis 取码证据失败时标记 `BLOCKED_MOCK_OTP_UNAVAILABLE`；取码成功但登录失败必须按真实产品/接口结果记录。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 登录成功后保存本轮 session，供 H5-007/008 和其他 runner 复用。
