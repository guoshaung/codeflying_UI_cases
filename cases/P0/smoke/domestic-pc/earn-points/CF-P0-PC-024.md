# CF-P0-PC-024 非新用户注册奖励不下发

- 优先级：P0
- 模块：国内主站 / 侧边栏 / 赚取积分
- 平台：国内PC
- 执行方式：可自动化
- 问题类型：产品体验
- 用户类型：免费用户 / 付费用户
- 前置条件：固定邀请人已登录；使用已经注册过的固定老用户手机号，验证不会再次下发新用户邀请奖励。若 OTP 已成功收到，不得因页面曾出现滑块就直接判定阻塞，继续完成登录和奖励校验。
- Agent 分组：invite_credit_agent
- 账号类型：fixed_inviter_account + fixed_existing_phone
- 是否修改数据：yes
- 数据锁：`invitation:{inviter_id}`
- 清理动作：不删除固定老用户；清理本轮可能产生的异常邀请关系或测试奖励记录
- 人工测试原因：无

## 测试步骤

1. 创建独立的 国内PC Browser Context，并按前置条件加载或清除登录态。
2. 进入 FM 路径：`国内主站 / 侧边栏 / 赚取积分`。
3. 执行原始测试点描述的操作：非新用户注册奖励不下发。
   - 使用固定邀请人复制邀请链接。
   - 使用固定老用户手机号通过邀请链接登录/尝试注册。
   - 记录邀请人积分前后变化和奖励流水查询结果。
4. 记录页面状态、URL、关键 DOM、接口结果和截图。

## 预期结果

1. 原始测试点描述的目标结果正确发生：非新用户注册奖励不下发。
2. 页面无白屏、无未处理报错，相关功能可继续操作。

## 通过标准

- 目标页面、弹窗、状态或数据变化与测试点描述一致。
- 固定老用户不会触发新用户奖励；邀请人积分不应因该老用户再次增加。
- 自动化证据完整；若有重试，必须记录首次错误并标记 `RETRY_PASS`。

## 失败条件

- 固定老用户触发新用户奖励、邀请人积分异常增加、出现重复奖励流水、接口报错或页面不可继续操作。

## 阻塞条件

- 测试环境、账号、第三方服务或必要测试数据不可用。

## 证据要求

- 操作前后截图。
- 页面 URL 与关键 DOM 状态。
- 相关业务接口结果；HTTP 200 仍需检查业务码与返回数据。
- 涉及数据变化时，保存变化前后数据及清理结果。

## 备注

- 本用例由 `码上飞冒烟测试用例.xmind` 自动拆分。
- 首次人工 review 时应补充稳定 DOM 选择器和更精确的页面入口。
